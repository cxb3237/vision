Removed from the touch webpage:
- all data-detector buttons and detector-selection requests
- digit, color, shape and classical steel-ball detector entry points
- detector name display
- target/class display
- confidence display
- mode display
- Center Y and the old combined center display
- competitionDock duplicate status panel
- overlayDetector, overlayTarget and overlayState
- DETECTOR_LABELS and detectorLabel
- classical-CV/YOLO display switching logic

Preserved:
- annotated camera preview
- runtime, camera and UART status
- position-packet activity/count
- camera, vision and preview FPS
- NCNN model status and inference timing
- capture-to-result latency and P95
- camera parameter drawer, save/restore and maintenance menu

Added:
- prominent signed ball_x_mm display
- `-- mm` for missing/unmapped position
- calibrated state and pixel-X diagnostic
- `启用位置下发` / `停止位置下发` toggle using existing competition APIs
